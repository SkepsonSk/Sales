{
  "view": {
    "fields": [
      {
        "field": "name",
        "name": "Name",
        "type": "text"
      }
    ]
  },
  "edit": {
    "fields": [

      {
        "field": "name",
        "name": "Name",
        "type": "text",
        "validations": [

          {
            "type": "regex",
            "regex": "^[A-Z]+[a-zA-Z]*$",
            "errorMessage": "Account name must start with capital letter."
          }

        ]
      }

    ]
  }
}
